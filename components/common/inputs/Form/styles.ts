import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  form: {
    alignSelf: "center",
    width: "100%",
    alignItems: "center",
    flex: 1,
    // backgroundColor: "yellow",
    // flexShrink: 1,
  },
});
